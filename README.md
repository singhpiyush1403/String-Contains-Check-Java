# String-Contains-Check-Java
A simple Java program to check if one string is contained within another using .contains() method. Prints a message based on whether the substring exists inside the main string.
